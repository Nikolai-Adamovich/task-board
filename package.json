{
  "name": "task-board",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "shared",
    "ui"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "npm run build --workspace=shared && npm run build --workspace=server && npm run build --workspace=ui",
    "build:shared": "npm run build --workspace=shared",
    "build:server": "npm run build --workspace=server",
    "build:ui": "npm run build --workspace=ui",
    "test": "npm test --workspace=server && npm test --workspace=ui",
    "test:verbose": "npm run test:verbose --workspace=server && npm run test:verbose --workspace=ui",
    "test:server": "npm test --workspace=server",
    "test:ui": "npm test --workspace=ui",
    "test:e2e": "npm run e2e --workspace=ui",
    "typecheck": "npm run typecheck --workspace=shared && npm run typecheck --workspace=server",
    "deploy:server": "cd server && npx wrangler deploy",
    "deploy:ui": "cd ui && npx wrangler pages deploy dist/ui/browser --project-name=task-board-ui",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "@eslint/js": "^9.0.0",
    "@eslint/markdown": "^8.0.3",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@typescript-eslint/eslint-plugin": "^8.63.0",
    "@typescript-eslint/parser": "^8.63.0",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0",
    "typescript": "~6.0.0",
    "typescript-eslint": "^8.0.0"
  },
  "allowScripts": {
    "bcrypt@6.0.0": true
  }
}
